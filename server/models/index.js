const User = require('./User');
const Provider = require('./Provider');
const Message = require('./Message');
const Appointment = require('./Appointment');
const Reply = require('./Reply');

module.exports = { User, Provider, Message, Appointment, Reply };
